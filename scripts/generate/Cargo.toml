[package]
name = "generate"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
pretty = { version = "=0.12.5", default-features = false }
# The version pretty measures text with, pinned alongside it.
unicode-width = "=0.2.2"

[build-dependencies]
serde_json = "1"
